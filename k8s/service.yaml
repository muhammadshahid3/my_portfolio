apiVersion: v1
kind: Service

metadata:
  name: static-web-app
  namespace: static-web-app

spec:
  type: ClusterIP

  selector:
    app: static-web-app

  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP